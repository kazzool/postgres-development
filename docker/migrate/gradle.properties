# When running gradle on the migrate container, use the following connection information.
local.flyway.url = jdbc:postgresql://database:5432/sample_db
local.flyway.user = flyway
local.flyway.password = flyway